%h3
  %b
    = @hotel.title

.col-md-6.col-md-offset-3
  - if @raiting.errors.any?
    #error_explanation.well
      %h2.panel-danger
        = pluralize(@raiting.errors.count, "error")
        prohibited
        raiting for this hotel from being saved:
      %ul.list-unstyled
        - @raiting.errors.full_messages.each do |msg|
          %li.label-danger= msg

.row
  .col-md-6
    %table.table.table-hover
      %thead
        %tr.danger
          %th.col-xs-1 Title
          %th.col-xs-6 Description
      %tbody
        %tr
          %td.col-xs-1 Stars:
          %td.col-xs-6= @hotel.stars
        %tr
          %td.col-xs-1 Users Raiting:
          %td.col-xs-6
            = @hotel.aver_raiting.nil? ? 0 : @hotel.aver_raiting.round(2)
        %tr
          %td.col-xs-1 Room description:
          %td.col-xs-6= @hotel.rooms
        %tr
          %td.col-xs-1 Price:
          %td.col-xs-6= @hotel.price
        %tr
          %td.col-xs-1 Breakfast:
          - if @hotel.breakfast
            %td.col-xs-6 present
          - else
            %td.col-xs-6 no breakfast
        %tr
          %td.col-xs-1 Foto:
          %td.col-xs-6= image_tag @hotel.foto.url.to_s, height: '100px' if @hotel.foto?
        %tr
          %td Address
          %td.col-xs-6
            %ul.list-unstyled
              %li
                Country: #{@hotel.address.country}
              %li
                State: #{@hotel.address.state}
              %li
                City: #{@hotel.address.city}
              %li
                Street: #{@hotel.address.street}
    %br/
    %h2 Comments
    = render @hotel.raitings
    %h2 Add a comment:
    - if user_signed_in?
      = render 'raitings/form'
    - else
      .well.well-sm
        %b= link_to 'Sign in', new_user_session_path
        %span or
        %b= link_to 'Sign up', new_user_registration_path
    %p= link_to 'Back to hotel list', hotels_path